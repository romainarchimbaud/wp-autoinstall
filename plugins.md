# Thème et plugins proposés à l'installation

Case cochée = pré-cochée dans la liste de `make setup` ; le mot entre backquotes est le slug (`wordpress.org/plugins/<slug>` ou `wordpress.org/themes/<slug>`), le reste est le libellé affiché.

## Thème
- [x] `generatepress` GeneratePress

## Plugins
- [x] `generateblocks` GenerateBlocks
- [x] `advanced-custom-fields` ACF
- [x] `better-wp-security` Kadence Security
- [x] `better-search-replace` Better Search Replace
- [x] `disable-comments` Disable Comments
- [x] `wordpress-seo` Yoast SEO
- [x] `wp-optimize` WP-Optimize
- [x] `updraftplus` UpdraftPlus
- [ ] `cookie-notice` Cookie Notice
